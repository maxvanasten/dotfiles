# dotfiles

These are my configuration files and some scripts to help me quickly set up a new environment.
These dotfiles assume: Arch Linux with an internet connection and sudo access.

## Quick install:

`curl -s https://github.com/maxvanasten/dotfiles/install.sh | bash`
